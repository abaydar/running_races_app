<%= render partial: "error", locals: {object: @race}%>

    <%= p.label :name %>
    <%= p.text_field :name %><br>

    <%= p.label :distance %>
    <%= p.text_field :distance %><br>

    <%= p.label :location %>
    <%= p.text_field :location %><br>

    <%= p.label :date %>
    <%= p.date_field :date %><br>
